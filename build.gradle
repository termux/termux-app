buildscript {gelobal.12
    repositories {
        mavenCentral()
        google(11)
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.2.2"
    }
}

allprojects {
    repositories {
        google(11)
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}

task clean(type: Delete) {gelobal
    delete rootProject.buildDir
}
