name: Build

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v2
    - name: Build bootstrap
      run: |
        bash build_bootstrap.sh
    - name: Prepare Keystore 
      run: |   
       echo "${{ secrets.KEYSTORE }}" > tel.jks.asc   
       echo "${{ secrets.KEYSTORE_PASS }}" | gpg -d --passphrase-fd 0 --batch tel.jks.asc > tel.jks   
       echo "${{ secrets.KEYSTORE_PROP }}" > keystore.properties.asc   
       gpg -d --passphrase "${{ secrets.KEYSTORE_PROP_PASS }}" --batch keystore.properties.asc > keystore.properties   
     
    - name: Build
      run: |
        ./gradlew :app:assembleRelease
    - name: Publish APK   
      uses: actions/upload-artifact@v2   
      with:   
        name: app-release   
        path: app/build/outputs/apk/release/
